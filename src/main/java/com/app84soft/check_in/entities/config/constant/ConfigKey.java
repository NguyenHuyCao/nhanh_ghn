package com.app84soft.check_in.entities.config.constant;

public enum ConfigKey {
    MAINTENANCE_MODE,

    ACCESS_TOKEN_TTL_MINUTES,
    REFRESH_TOKEN_TTL_DAYS,
    PASSWORD_RESET_TOKEN_TTL_MINUTES,

    MAX_UPLOAD_SIZE_MB,

    SMTP_HOST,
    SMTP_PORT,
    SMTP_USERNAME,
    SMTP_FROM
}
